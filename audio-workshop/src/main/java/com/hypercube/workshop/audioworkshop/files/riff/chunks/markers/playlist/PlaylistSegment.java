package com.hypercube.workshop.audioworkshop.files.riff.chunks.markers.playlist;

public record PlaylistSegment(long dwIdentifier,
                              long dwLength,
                              long dwRepeats) {
}
