package com.hypercube.workshop.audioworkshop.files.riff.chunks.gig;

/**
 * @param type        No computed for now
 * @param bits        No computed for now
 * @param zones       No computed for now
 * @param splitType   No computed for now
 * @param zone_size   No computed for now
 * @param sampleIndex index in the sample pool
 */
public record G3Dimension(int type, int bits, int zones, int splitType, int zone_size, int sampleIndex) {

}
