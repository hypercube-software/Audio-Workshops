package com.hypercube.workshop.midiworkshop.common.presets;

public enum MidiPresetNaming {
    /**
     * Preset names can be retrieved via MIDI
     */
    STANDARD,
    /**
     * Preset names cannot be retrieved via MIDI, they need to be hardcoded
     */
    SOUND_CANVAS,
    /**
     * Preset names cannot be retrieved via MIDI, they need to be hardcoded
     */
    YAMAHA_XG
}
